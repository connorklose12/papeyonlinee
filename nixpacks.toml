[variables]
PIP_BREAK_SYSTEM_PACKAGES = "1"

[phases.setup]
nixPkgs = ["python310"]

[phases.install]
cmds = ["python3 -m ensurepip --upgrade", "pip install -r requirements.txt"]

[start]
cmd = "daphne -b 0.0.0.0 -p $PORT papeyonlinee.asgi:application"
