[phases.setup]
nixPkgs = ["python310", "nodejs-18_x"]

[phases.install]
cmds = ["npm ci", "python3 -m ensurepip --upgrade", "pip install -r requirements.txt"]

[start]
cmd = "daphne -b 0.0.0.0 -p $PORT mygame.asgi:application"
